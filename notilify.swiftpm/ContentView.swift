import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Welcome to Notilify!")
                .font(.largeTitle)
                .padding()
            Text("Tap on an option to get the calculator you'd like")
        }
    }
}
